#include "bases/engine_solution.sr"

mfwengine : base_solution
{
	projects {
		stl
		core
		launcher
		renderer
		//scripting
		engine
	}

	plugin::debug {
		program "${package_folder}/launcher"
		cwd "${package_folder}"
		type "gdb"
	}
}
